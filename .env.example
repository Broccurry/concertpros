# Copy to .env for local work (gitignored — never commit real credentials).
# On Railway itself these are injected automatically for a linked service.
DATABASE_URL=postgresql://user:password@host:port/railway
